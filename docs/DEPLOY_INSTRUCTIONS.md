# 🚀 راهنمای Deploy خودکار 

## خلاصه

حالا پروژه شما **کاملاً خودکار** است! هیچ دستور دستی لازم نیست.

## 📋 Deploy کامل با یک دستور

```bash
# اجرای مجوز (فقط اولین بار)
chmod +x auto_deploy.sh

# Deploy کامل خودکار
./auto_deploy.sh
```

**همین!** 🎉 سیستم همه کارها را خودکار انجام می‌دهد:

## ✅ آنچه خودکار انجام می‌شود:

1. **🔄 Git Pull** - دریافت آخرین تغییرات
2. **🐳 Docker Build** - ساخت containers جدید  
3. **🗄️ Migrations** - اجرای خودکار migration ها
4. **⚙️ Celery Services** - راه‌اندازی Worker & Beat
5. **📅 Token Schedule** - تنظیم خودکار زمان‌بندی رفرش
6. **🔍 Health Check** - بررسی سلامت کل سیستم

## 🕒 زمان‌بندی خودکار توکن‌ها:

- **🌙 روزانه ساعت 3 صبح:** رفرش توکن‌های 7 روز مانده
- **⚡ هر 6 ساعت:** رفرش اضطراری 1 روز مانده  
- **🔄 Real-time:** بازیابی فوری هنگام ارسال پیام

## 📊 دستورات مفید (اختیاری):

```bash
# بررسی وضعیت کلی
docker compose ps

# نظارت بر لاگ‌ها
docker logs django_app --tail 50
docker logs celery_worker --tail 50
docker logs celery_beat --tail 50

# بررسی task های رفرش
docker exec django_app python manage.py shell -c "
from django_celery_beat.models import PeriodicTask
for task in PeriodicTask.objects.filter(name__icontains='Instagram'):
    print(f'{task.name}: {\"✅ Enabled\" if task.enabled else \"❌ Disabled\"}')
"
```

## 🔧 در صورت مشکل:

```bash
# راه‌اندازی مجدد سریع
docker compose restart

# ریبیلد کامل
docker compose down
docker compose up -d --build
```

## 🎯 نتیجه:

**سیستم کاملاً خودکار است:**
- ✅ هیچ دستور دستی لازم نیست
- ✅ توکن‌ها خودکار رفرش می‌شوند
- ✅ در صورت خطا، خودکار بازیابی می‌شوند
- ✅ هیچ انقضا یا قطعی سرویس نخواهید داشت

---

## 🚀 خلاصه برای Deploy:

**فقط این دو دستور کافی است:**

```bash
chmod +x auto_deploy.sh
./auto_deploy.sh
```

بقیه کارها خودکار! 🎉