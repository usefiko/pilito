# 🤖 راهنمای GitHub Actions برای Deploy خودکار

## خلاصه

GitHub Actions شما حالا **کاملاً خودکار** است و تمام کارهای لازم را انجام می‌دهد.

## ✅ آنچه GitHub Actions انجام می‌دهد:

### 🔄 **هر بار Push به `main`:**

1. **📥 Code Transfer** - انتقال کد به سرور
2. **🔨 Build Containers** - ساخت containers با requirements جدید
3. **⚡ Start Services** - راه‌اندازی Django + Celery + Redis + PostgreSQL
4. **🗄️ Auto Migrations** - اجرای خودکار migrations (در entrypoint)
5. **📅 Setup Token Refresh** - تنظیم خودکار periodic tasks (در apps.py)
6. **🔍 Health Checks** - بررسی سلامت کل سیستم
7. **✅ Verification** - تأیید عملکرد token refresh system

## 🎯 **نتیجه:**

### فقط یک کار لازم است:

```bash
git add .
git commit -m "your message"
git push origin main
```

**بقیه خودکار!** 🎉

## 📊 **مراحل خودکار Deploy:**

```
📤 Git Push
    ↓
🤖 GitHub Actions شروع می‌شود
    ↓
📥 Code به سرور منتقل می‌شود
    ↓
🔨 Docker containers rebuild می‌شوند
    ↓
⚡ Services restart می‌شوند
    ↓
🗄️ Migrations خودکار اجرا می‌شوند
    ↓
📅 Token refresh خودکار setup می‌شود
    ↓
🔍 Health checks انجام می‌شود
    ↓
✅ Deploy موفق!
```

## 🕒 **زمان‌بندی خودکار Token ها:**

- **🌙 روزانه ساعت 3 صبح:** رفرش توکن‌های 7 روز مانده
- **⚡ هر 6 ساعت:** رفرش اضطراری 1 روز مانده
- **🔄 Real-time:** بازیابی فوری هنگام خطا

## 📋 **مانیتورینگ (اختیاری):**

### بررسی Actions:
- GitHub → Repository → Actions tab
- مشاهده لاگ‌های deploy

### بررسی سرور:
```bash
# SSH به سرور
ssh ubuntu@your-server

# بررسی وضعیت
cd /home/ubuntu/fiko-backend
docker-compose ps

# بررسی لاگ‌ها
docker logs django_app --tail 50
docker logs celery_worker --tail 50
docker logs celery_beat --tail 50
```

## 🚨 **در صورت مشکل:**

### GitHub Actions خطا داد:
1. Actions tab → مشاهده لاگ خطا
2. اصلاح مشکل در کد
3. Push مجدد

### سرور مشکل دارد:
```bash
# SSH به سرور
ssh ubuntu@your-server
cd /home/ubuntu/fiko-backend

# Restart سریع
docker-compose restart

# یا rebuild کامل
docker-compose down
docker-compose up -d --build
```

## 🎉 **خلاصه:**

**شما دیگر نیازی به کار دستی ندارید!**

✅ فقط کد بنویسید و push کنید
✅ GitHub Actions همه چیز را خودکار انجام می‌دهد  
✅ توکن‌های Instagram خودکار رفرش می‌شوند
✅ هیچ انقضا یا قطعی سرویس نخواهید داشت

**سیستم کاملاً خودکار است!** 🚀