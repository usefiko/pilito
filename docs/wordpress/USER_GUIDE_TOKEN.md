# 📖 راهنمای کاربر - ساخت Token برای WooCommerce

## 🎯 برای کاربران عادی

### روش 1️⃣: از داشبورد پیلیتو (Frontend - آسان‌ترین)

**این endpoint باید در Frontend پیاده بشه:**

```
صفحه: تنظیمات > ادغام‌ها > فروشگاه

[دکمه: ایجاد Token برای فروشگاه]

کاربر روی دکمه کلیک می‌کنه:
  ↓
Frontend می‌فرسته:
  POST /api/v1/integrations/tokens/generate/
  
Response:
{
  "token": "wc_sk_live_abc123...",  ← این رو نمایش می‌ده
  "message": "⚠️ این token فقط یکبار نمایش داده می‌شود"
}

[Token نمایش داده می‌شه با دکمه کپی]
```

---

### روش 2️⃣: از API مستقیم (Postman, curl)

```bash
# درخواست با JWT token کاربر
curl -X POST https://api.pilito.com/api/v1/integrations/tokens/generate/ \
  -H "Authorization: Bearer USER_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "integration_type": "woocommerce",
    "name": "فروشگاه من"
  }'

# Response:
{
  "id": "uuid",
  "token": "wc_sk_live_...",  ← کپی کنید
  "token_preview": "wc_sk...abc",
  "integration_type": "woocommerce",
  "name": "فروشگاه من",
  "user": {
    "id": 123,
    "email": "user@example.com"
  },
  "message": "⚠️ این token فقط یکبار نمایش داده می‌شود. لطفاً آن را کپی کنید."
}
```

---

## 👨‍💼 برای Admin

### از Django Admin:

1. برو به: `https://api.pilito.com/admin/integrations/integrationtoken/`
2. **Add Integration Token** بزن
3. فیلدها رو پر کن:
   - User: انتخاب کاربر
   - Name: نام دلخواه (مثلاً "فروشگاه اصلی")
   - Integration type: WooCommerce
4. **Save**
5. بعد از ذخیره، صفحه باز می‌شه و **Token کامل** نمایش داده می‌شه
6. Token رو کپی کن

یا:

1. لیست tokenها رو ببین
2. یکی رو انتخاب کن
3. از Actions: **🔑 ساخت Token جدید برای انتخاب شده**
4. Go
5. Token جدید در پیام سبز بالای صفحه نمایش داده می‌شه

---

## 🔐 Token موجود (برای تست):

```
wc_sk_live_1hkzmml41b4lvlts0faqjkonyxcwvqt1euf1ee0o
```

این token برای کاربر `admin@admin.com` است.

---

## 📱 نمایش در Frontend (پیشنهاد UI)

### صفحه تنظیمات > ادغام‌ها

```
┌─────────────────────────────────────────────────┐
│ 🛒 فروشگاه آنلاین (WooCommerce)                 │
├─────────────────────────────────────────────────┤
│                                                 │
│ محصولات فروشگاه خود را با پیلیتو هماهنگ کنید   │
│ تا هوش مصنوعی بتواند به مشتریان پاسخ دهد.      │
│                                                 │
│ [دکمه: ایجاد Token برای فروشگاه]                │
│                                                 │
│ توکن‌های فعال:                                   │
│ ┌───────────────────────────────────────────┐   │
│ │ 🔑 فروشگاه اصلی                            │   │
│ │ wc_sk...abc123                            │   │
│ │ آخرین استفاده: 2 ساعت پیش                 │   │
│ │ [حذف]                                     │   │
│ └───────────────────────────────────────────┘   │
└─────────────────────────────────────────────────┘
```

### Modal هنگام ایجاد Token

```
┌─────────────────────────────────────────┐
│ ✅ Token با موفقیت ایجاد شد             │
├─────────────────────────────────────────┤
│                                         │
│ ⚠️ این token فقط یکبار نمایش داده      │
│ می‌شود. حتماً آن را کپی کنید!          │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ wc_sk_live_abc123def456...          │ │
│ │ [📋 کپی]                            │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ 📌 مراحل بعدی:                          │
│ 1. این token را کپی کنید               │
│ 2. به پنل WordPress بروید               │
│ 3. Plugins > Pilito Product Sync       │
│ 4. Token را در تنظیمات قرار دهید        │
│                                         │
│ [متوجه شدم، بستن]                       │
└─────────────────────────────────────────┘
```

---

## 🎨 نمونه کد React

```jsx
import { useState } from 'react'

function IntegrationSettings() {
  const [token, setToken] = useState(null)
  const [loading, setLoading] = useState(false)
  
  const generateToken = async () => {
    setLoading(true)
    
    const response = await fetch('/api/v1/integrations/tokens/generate/', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${userJWT}`,
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        integration_type: 'woocommerce',
        name: 'فروشگاه اصلی'
      })
    })
    
    const data = await response.json()
    
    if (data.token) {
      setToken(data.token)
      // نمایش modal با token
    }
    
    setLoading(false)
  }
  
  return (
    <div>
      <h2>🛒 فروشگاه آنلاین</h2>
      <button onClick={generateToken} disabled={loading}>
        {loading ? 'در حال ایجاد...' : 'ایجاد Token برای فروشگاه'}
      </button>
      
      {token && (
        <div className="token-modal">
          <h3>✅ Token ایجاد شد</h3>
          <code>{token}</code>
          <button onClick={() => navigator.clipboard.writeText(token)}>
            📋 کپی
          </button>
        </div>
      )}
    </div>
  )
}
```

---

## 📋 API Endpoints برای Frontend

### 1. لیست tokenهای کاربر

```http
GET /api/v1/integrations/tokens/
Authorization: Bearer {user_jwt}

Response:
{
  "count": 2,
  "results": [
    {
      "id": "uuid",
      "name": "فروشگاه اصلی",
      "integration_type": "woocommerce",
      "token_preview": "wc_sk...abc123",
      "is_active": true,
      "usage_count": 150,
      "last_used_at": "2025-11-10T10:00:00Z",
      "created_at": "2025-11-01T10:00:00Z"
    }
  ]
}
```

### 2. ساخت token جدید

```http
POST /api/v1/integrations/tokens/generate/
Authorization: Bearer {user_jwt}
Content-Type: application/json

Body:
{
  "integration_type": "woocommerce",
  "name": "فروشگاه اصلی"
}

Response:
{
  "id": "uuid",
  "token": "wc_sk_live_...",  ← فقط یکبار!
  "token_preview": "wc_sk...abc",
  "name": "فروشگاه اصلی",
  "message": "⚠️ این token فقط یکبار نمایش داده می‌شود"
}
```

### 3. حذف token

```http
DELETE /api/v1/integrations/tokens/{id}/
Authorization: Bearer {user_jwt}

Response: 200 OK
```

---

## ✅ داکیومنت Frontend آماده است:

📄 `docs/wordpress/WOOCOMMERCE_FRONTEND_API.md`

شامل:
- ✅ API برای لیست محصولات
- ✅ فیلترها (external_source, category, price, ...)
- ✅ جستجو
- ✅ Pagination
- ✅ نمونه React Components

---

**همه چیز آماده است! فقط باید در Frontend یک صفحه settings داشته باشی که کاربر token بسازه.** 🎉

