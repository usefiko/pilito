# راهنمای انتشار پلاگین در WordPress.org

## 📋 اطلاعات لازم برای فرم

### 1. Additional Information (اطلاعات اضافی)

```
این پلاگین برای همگام‌سازی محتوای سایت وردپرس (محصولات WooCommerce، برگه‌ها و نوشته‌ها) با پلتفرم پیلیتو طراحی شده است.

ویژگی‌های اصلی:
- همگام‌سازی خودکار محصولات WooCommerce
- همگام‌سازی برگه‌ها و نوشته‌های وردپرس
- رابط کاربری ساده و حرفه‌ای
- پشتیبانی از زبان‌های فارسی و انگلیسی
- بدون نیاز به تنظیمات پیچیده

پلاگین کاملاً رایگان و متن‌باز است و تحت مجوز GPL v2 منتشر می‌شود.
```

### 2. تیک‌های لازم (همه باید زده بشن):

✅ **I have read the Frequently Asked Questions**
- بله، تیک بزن

✅ **This plugin complies with all of the Plugin Developer Guidelines**
- بله، تیک بزن
- پلاگین استانداردهای وردپرس رو رعایت می‌کنه

✅ **I have permission to upload this plugin to WordPress.org for others to use and share**
- بله، تیک بزن
- شما صاحب کد هستید

✅ **This plugin, all included libraries, and any other included assets are licenced as GPL or are under a GPL compatible license**
- بله، تیک بزن
- پلاگین تحت مجوز GPL v2 هست

✅ **I confirm that the plugin has been tested with the Plugin Check plugin, and all indicated issues resolved**
- **مهم**: قبل از آپلود، حتماً پلاگین رو با Plugin Check تست کن
- پلاگین Plugin Check رو از وردپرس نصب کن و تست کن
- اگر خطایی بود، اول رفع کن بعد آپلود کن

## 📦 فایل‌های لازم

### فایل اصلی:
- `pilito-sync.zip` (فایل زیپ شده پلاگین)

### محتویات فایل زیپ باید شامل:
```
pilito-sync/
├── pilito-sync.php (فایل اصلی پلاگین)
├── readme.txt (فایل readme استاندارد)
├── uninstall.php
├── admin/
├── includes/
├── assets/
└── ...
```

## ⚠️ نکات مهم قبل از آپلود

### 1. تست با Plugin Check:
```
1. پلاگین Plugin Check رو از وردپرس نصب کن
2. Tools > Plugin Check > Check Plugin
3. فایل pilito-sync.zip رو آپلود کن
4. تمام خطاها رو رفع کن
```

### 2. بررسی readme.txt:
- باید فرمت استاندارد WordPress.org باشه
- Stable tag باید درست باشه
- تمام بخش‌ها باید پر شده باشن

### 3. بررسی فایل اصلی:
- Header پلاگین باید کامل باشه
- Version باید درست باشه
- Text Domain باید درست باشه

### 4. بررسی امنیت:
- تمام ورودی‌های کاربر باید sanitize بشن
- تمام خروجی‌ها باید escape بشن
- از nonce برای امنیت استفاده شده

## 📝 چک‌لیست نهایی

قبل از آپلود، این موارد رو چک کن:

- [ ] فایل readme.txt استاندارد هست
- [ ] Version در فایل اصلی و readme.txt یکسان هست
- [ ] تمام کدها sanitize و escape شدن
- [ ] با Plugin Check تست شده و خطایی نداره
- [ ] فایل زیپ درست ساخته شده (بدون فایل‌های اضافی)
- [ ] تمام متن‌ها برای ترجمه آماده شدن (i18n)
- [ ] پلاگین در وردپرس 5.8+ تست شده
- [ ] پلاگین در PHP 7.4+ تست شده

## 🚀 مراحل آپلود

1. **آماده‌سازی:**
   - فایل `pilito-sync.zip` رو آماده کن
   - با Plugin Check تست کن

2. **پر کردن فرم:**
   - Additional Information رو پر کن
   - همه تیک‌ها رو بزن

3. **آپلود:**
   - فایل زیپ رو آپلود کن
   - Submit کن

4. **منتظر بمان:**
   - حداقل 14 روز منتظر بمان
   - اگر مشکلی بود، ایمیل می‌فرستن

## 📧 اطلاعات تماس

اگر سوالی داشتی یا مشکلی پیش اومد:
- ایمیل: از ایمیل حساب WordPress.org استفاده می‌کنن
- Support Forum: بعد از تایید، می‌تونی در forum پشتیبانی کنی

## ⚡ نکات اضافی

1. **SVG Icons**: اگر از SVG استفاده می‌کنی، باید sanitize بشه
2. **External APIs**: اگر از API خارجی استفاده می‌کنی، باید در readme ذکر بشه
3. **Dependencies**: اگر وابستگی خاصی داری (مثل WooCommerce)، باید optional باشه
4. **Translations**: اگر می‌خوای ترجمه داشته باشه، باید فایل‌های .pot آماده کنی

